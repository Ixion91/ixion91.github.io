function sendMail()
{
    var firstName = document.getElementById ("firstName").value;
    var lastName = document.getElementById ("lastName").value;
    var emailAddress = document.getElementById ("emailAddress").value;
    var phone = document.getElementById ("phone").value;
    window.location.href = "mailto:tomas.esquivel@ice.com?subject=Pre-Qualification Request for "+firstName+"&body=Hi my name is "+firstName+" "+lastName+".%0D%0AI am interested in a pre-qualification. My contact information is:%0D%0AEmail: "+emailAddress+"%0D%0APhone Number: "+phone;
}
